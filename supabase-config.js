/* ============================================================
   إعدادات Supabase — لتخزين صور المسودات فقط
   ------------------------------------------------------------
   ⚠️  مشروع منفصل تماماً عن Supabase تبع الموقع/الأدمن الحقيقيين.
       ما تستخدم بيانات مشروعهم هون إطلاقاً — أنشئ مشروعاً جديداً
       مستقلاً خاصاً بهذه المسودات فقط.

   لماذا Supabase هون وFirebase بالباقي؟
   الصور فقط تُخزَّن هون (كملفات حقيقية، رابط قصير يُحفظ بـ Firestore).
   بيانات المسودة نفسها (العنوان، السعر، الوصف...) تبقى بـ Firestore
   متل ما هي — هاد الملف خاص بالصور بس.

   كيف تنشئ المشروع وتملأ القيم:
   1) افتح https://supabase.com/dashboard ← New project
      اختر اسماً (مثلاً mosawada-storage) وكلمة سر لقاعدة البيانات
      واحفظها بمكان آمن (ما رح تحتاجها هون، بس احتفظ فيها).
   2) بعد الإنشاء: من القائمة الجانبية Storage → New bucket
      الاسم: drafts   |  فعّل خيار Public bucket
   3) بعد إنشاء الـ bucket، افتح تبويب Policies جوّا الـ bucket
      واضغط New policy → اختر "For full customization" والصق:

        -- قراءة عامة (لعرض الصور بالتطبيق)
        create policy "قراءة عامة لصور المسودات"
        on storage.objects for select
        using (bucket_id = 'drafts');

        -- رفع من أي حدا يحمل مفتاح anon (نفس حماية Firestore —
        -- الحماية الحقيقية هي شاشة تسجيل الدخول بالتطبيق نفسه)
        create policy "رفع صور المسودات"
        on storage.objects for insert
        with check (bucket_id = 'drafts');

   4) Project Settings (⚙) → Data API → انسخ Project URL
      Project Settings → API Keys → انسخ مفتاح anon public
      والصقهم تحت.

   إذا تركت القيم فارغة، رفع الصور ما رح يشتغل وبتظهر رسالة خطأ واضحة
   عند محاولة إضافة صورة — باقي التطبيق (النموذج، الحفظ، المعلقات)
   بيشتغل عادي بلا صور لحد ما تعبّي هالملف.
   ============================================================ */

window.SUPABASE_CONFIG = {
  url: "",
  anonKey: "",
};

/* اسم الـ bucket المخصَّص لصور المسودات — لا داعي لتغييره */
window.DRAFTS_BUCKET = "drafts";
